seed: 0
batch_size: 1
effective_batch_size: 1
num_workers: 0
max_epochs: 1
effective_steps_per_epoch: 300
train_log_every_n_steps: 10
visualize_every_n_steps: 100
grad_clip_norm: 1.0
lr: 2.0e-5
backbone_lr_mult: 0.1
weight_decay: 0.0
optimizer: adamw
lr_scheduler:
  name: constant