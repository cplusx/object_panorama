seed: 0
batch_size: 2
num_workers: 4
max_steps: 10000
log_every: 10
save_every: 200
val_every: 200
visualize_every: 100
grad_clip_norm: 1.0
lr: 1.0e-4
backbone_lr_mult: 0.1
weight_decay: 1.0e-5
optimizer: adamw
lr_scheduler:
  name: cosine_with_warmup
  warmup_steps: 200