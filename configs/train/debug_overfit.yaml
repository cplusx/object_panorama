seed: 0
batch_size: 2
num_workers: 0
max_steps: 200
log_every: 10
save_every: 50
val_every: 0
visualize_every: 50
grad_clip_norm: 1.0
lr: 3.0e-4
backbone_lr_mult: 0.1
weight_decay: 0.0
optimizer: adamw
lr_scheduler:
  name: constant